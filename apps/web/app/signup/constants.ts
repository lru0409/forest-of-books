export const TOTAL_STEPS = 3;

export enum Step {
  EMAIL_PASSWORD = 1,
  PROFILE = 2,
  GENRES = 3,
}

export const PROFILE_IMAGE_ACCEPTED_TYPES = ['image/jpeg', 'image/png', 'image/webp', 'image/gif'];
export const PROFILE_IMAGE_MAX_FILE_SIZE = 5 * 1024 * 1024; // 5MB
